import { DltIfDirective } from './dlt-if.directive';

describe('DltIfDirective', () => {
  it('should create an instance', () => {
    const directive = new DltIfDirective();
    expect(directive).toBeTruthy();
  });
});
