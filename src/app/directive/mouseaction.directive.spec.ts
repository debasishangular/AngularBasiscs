import { MouseactionDirective } from './mouseaction.directive';

describe('MouseactionDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseactionDirective();
    expect(directive).toBeTruthy();
  });
});
