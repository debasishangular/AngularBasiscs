import { DltForLoopDirective } from './dlt-for-loop.directive';

describe('DltForLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new DltForLoopDirective();
    expect(directive).toBeTruthy();
  });
});
